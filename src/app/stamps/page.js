export default function Stamps() {
  return (
  <>
    <h1 className='text-center'>My Stamps</h1>
    <div className="grid grid-cols-2 gap-6 px-4">
      <div
        className="w-40 h-40 rounded-full border-2 border-dotted flex items-center justify-center text-lg"
        style={{ borderColor: '#C09635' }}
      >
      </div>
      <div
        className="w-40 h-40 rounded-full border-2 border-dotted flex items-center justify-center text-lg"
        style={{ borderColor: '#C09635' }}
      >
      </div>
      <div
        className="w-40 h-40 rounded-full border-2 border-dotted flex items-center justify-center text-lg"
        style={{ borderColor: '#C09635' }}
      >
      </div>
      <div
        className="w-40 h-40 rounded-full border-2 border-dotted flex items-center justify-center text-lg"
        style={{ borderColor: '#C09635' }}
      >
      </div>
      <div
        className="w-40 h-40 rounded-full border-2 border-dotted flex items-center justify-center text-lg"
        style={{ borderColor: '#C09635' }}
      >
      </div>
      <div
        className="w-40 h-40 rounded-full border-2 border-dotted flex items-center justify-center text-lg"
        style={{ borderColor: '#C09635' }}
      >
      </div>
      <div
        className="w-40 h-40 rounded-full border-2 border-dotted flex items-center justify-center text-lg"
        style={{ borderColor: '#C09635' }}
      >
      </div>
      <div
        className="w-40 h-40 rounded-full border-2 border-dotted flex items-center justify-center text-lg"
        style={{ borderColor: '#C09635' }}
      >
      </div>
      <div
        className="w-40 h-40 rounded-full border-2 border-dotted flex items-center justify-center text-lg"
        style={{ borderColor: '#C09635' }}
      >
      </div>
      <div
        className="w-40 h-40 rounded-full border-2 border-dotted flex items-center justify-center text-lg"
        style={{ borderColor: '#C09635' }}
      >
      </div>
      <div
        className="w-40 h-40 rounded-full border-2 border-dotted flex items-center justify-center text-lg"
        style={{ borderColor: '#C09635' }}
      >
      </div>
      <div
        className="w-40 h-40 rounded-full border-2 border-dotted flex items-center justify-center text-lg"
        style={{ borderColor: '#C09635' }}
      >
      </div>
    </div>
  </>
  );
}
